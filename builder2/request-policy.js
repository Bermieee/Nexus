export const BUILDER2_REQUEST_INTENT=Object.freeze({BUILD:'build',VALIDATE_ONLY:'validate-only'});
export function createBuilder2RequestPolicy({validateOnly=false,requestedBy='operator',allowSemantic=true}={}){const intent=validateOnly?BUILDER2_REQUEST_INTENT.VALIDATE_ONLY:BUILDER2_REQUEST_INTENT.BUILD;return Object.freeze({intent,validateOnly:intent===BUILDER2_REQUEST_INTENT.VALIDATE_ONLY,requestedBy:String(requestedBy||'operator'),semanticAllowed:allowSemantic!==false,previewAllowed:true,ledgerStageAllowed:intent===BUILDER2_REQUEST_INTENT.BUILD,canonicalCommitAllowed:intent===BUILDER2_REQUEST_INTENT.BUILD});}
export function assertBuilder2SemanticAllowed(p){if(!p?.semanticAllowed)throw new Error('Builder 2 semantic execution is disabled by request policy.');return p;}export function assertBuilder2CanStage(p){if(p?.validateOnly||p?.ledgerStageAllowed!==true)throw new Error('Builder 2 validateOnly/read-only requests cannot stage a Ledger mutation.');return p;}export function assertBuilder2CanCommit(p){if(p?.validateOnly||p?.canonicalCommitAllowed!==true)throw new Error('Builder 2 validateOnly/read-only requests cannot enter canonical commit.');return p;}export function finalizeBuilder2ReadOnlyResult(p,r={}){if(!p?.validateOnly)return structuredClone(r);const o=structuredClone(r);for(const k of ['commitEnvelope','ledgerArtifact','stagedArtifact','canonicalMutation','mutation'])delete o[k];return{...o,validateOnly:true,readOnly:true,staged:false,committed:false};}
